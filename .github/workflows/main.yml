name: check-build

on: [pull_request]

jobs:

  project: 
     runs-on: ubuntu-latest
   
     steps: 
        - name: go-to-project
          run: ls -l
          
          
  init: 
     runs-on: ubuntu-latest
     needs: [project]
     steps: 
        - name: init-project
          run: yarn init --yes

  eslint: 
     runs-on: ubuntu-latest
     needs: [init]
     steps: 
        - name: check-lint
          run: yarn lint
          
            
