name: check-build,

on: [pull_request]
  
jobs:
  init: 
     runs-on: ubuntu-latest
     
     steps:             
        - name: init-app
          run: |
               git clone https://github.com/Andry-art/Front-steps.git
               ls
         
               
          
          
