name: check-build,

on: [pull_request]
  
jobs:
  init: 
     runs-on: ubuntu-latest
   
     steps:             
        - name: init-app
          working-directory: ./stepsApp
          run: pwd
          
          
  eslint: 
        runs-on: ubuntu-latest
        needs: [ init ]
        steps: 
           - name: check-eslint
             run: yarn lint
